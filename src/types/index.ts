/**
 * Type definitions index.ts
 *
 * Overview of all available types in one place:
 * - Employee: Used in Form and TableData
 * - Employee header: Used in TableData
 * - Options: Used for Select
 */

// Employee
export type { EmployeeType, EmployeesType } from '../schemas/employeeSchema';

// Employee header
export type {
  EmployeeHeaderType,
  EmployeeHeadersType,
} from '../schemas/employeeHeaderSchema';

// Option
export type { OptionType, OptionsType } from '../schemas/optionSchema';
